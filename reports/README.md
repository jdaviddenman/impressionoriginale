# Reports

Before/after harness output lands here once the clone finishes importing and the fix is trialled.

Planned:

- `baseline-clone.txt` — clone fingerprint before any change (parity-checked against live).
- `round1-wpml.txt` — after updating the WPML family.
- `round2-yoast.txt` — after updating Yoast.
- `diff-baseline-vs-final.txt` — the delta proving hreflang appeared and nothing regressed.
